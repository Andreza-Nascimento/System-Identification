function ahat = ols_est(sinal,p,n)
    x = sinal';
    v = x(p+1:n);
    u = p+1;
    X = [];
    for k=1:p
        x_n = x(p+k)
        X(:,u-k) = x(n+(u-k):k);
    end
    ahat = (X'*X)\(X'*v);
end
